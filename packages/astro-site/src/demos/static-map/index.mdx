---
name: Static Map with Path Generation
description: Loads a static map background and listens for user clicks to generate a path.
tags: ['maplibre', 'path', 'map']
---
import StaticMap from './loader.astro'
import './style.css'

# Static Map with MapLibre

Loads a static map background in svg instead of the standard tile set.

Click anywhere to start assembling a map path.

<StaticMap />
