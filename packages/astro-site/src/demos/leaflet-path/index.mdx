---
name: Leaflet Path
description: Demonstrates the use of Leaflet Path components in a map.
tags: ['leaflet', 'path', 'map']
---
import LeafletPath from './leaflet-path.astro';
import './style.css'

# Leaflet Path

## Click anywhere on the map to add a new point to the path.
### Move path points by dragging them.

<LeafletPath
  title="Leaflet Path"
  description="This demo showcases the use of Leaflet Path components in a map."
  pathData={[
    { lat: 51.505, lng: -0.09 },
    { lat: 51.51, lng: -0.1 },
    { lat: 51.51, lng: -0.12 },
    { lat: 51.505, lng: -0.09 }
  ]}
  options={{
    color: 'blue',
    weight: 5,
    opacity: 0.7,
    fillColor: 'blue',
    fillOpacity: 0.2
  }}
  mapOptions={{
    center: [51.505, -0.09],
    zoom: 13,
    maxZoom: 18,
    minZoom: 2
  }}
  mapStyle={{
    height: '500px',
    width: '100%'
  }}
  mapContainerStyle={{
    position: 'relative',
    height: '500px',
    width: '100%'
  }}
  mapClassName="leaflet-map"
  mapId="leaflet-path-map"
  mapTitle="Leaflet Path Map" />